server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # API → Backend
    location /api/ {
        proxy_pass http://backend:8000;
    }

    # Frontend Routing
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}